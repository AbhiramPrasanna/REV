# Keep shell scripts and runtime config as LF so they run on Linux even when the
# repo is edited/checked out on Windows (CRLF would cause "bad interpreter ^M").
*.sh        text eol=lf
*.conf      text eol=lf
memcached.conf text eol=lf
script/*    text eol=lf
